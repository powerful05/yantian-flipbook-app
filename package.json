{
  "name": "yantian-flipbook-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env.local server.mjs",
    "dev:models": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/start-with-model-key.ps1",
    "start": "node --env-file-if-exists=.env.local server.mjs",
    "test": "node --test",
    "validate:dataset": "node scripts/validate-p0-dataset.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  }
}
