# 盐田 Flipbook 领域词汇

本文件记录“图片即界面”的领域语言，避免把传统 PDF 翻页、模型生成页面和实时视频混为一个概念。

## 探索模型

**探索会话（Session）**：一组可继续、分享和分叉的视觉探索上下文。  
_避免_：把一次 HTTP 请求称作会话。

**场景节点（SceneNode）**：一个稳定的视觉页面及其生成收据、语义锚点和父子关系。  
_避免_：把图片文件直接称作页面节点。

**探索边（Edge）**：从父节点中的语义锚点到子节点的导航关系，记录动作和坐标。  
_避免_：把所有点击都称作新页面。

**语义锚点（SemanticAnchor）**：画面中可被点选、解释和复用的对象，至少包含名称、类别和归一化位置。  
_避免_：把图片里偶然生成的文字当作锚点数据。

## 资源与解释

**画面资源（RenderAsset）**：草稿、最终图片或缩略图等视觉文件。  
_避免_：把资源 URL 当作节点身份。

**动效资源（MotionAsset）**：从画面资源派生的短视频或 fMP4 片段，可失败、可重建、可缓存。  
_避免_：把视频生成任务当作节点本身。

**标签解释（Explanation）**：对语义锚点的简短说明、事实、来源和置信度。  
_避免_：把模型自由发挥的描述直接当成事实。

**模型收据（ModelReceipt）**：记录实际模型版本、参数摘要、耗时、成本、失败和降级结果。  
_避免_：只记录“使用了国产模型”这种不可审计的标签。

## 交互状态

**即时过渡（Instant Transition）**：浏览器本地完成的缩放、平移、视差或淡入，用来隐藏等待。  
_避免_：把本地动画宣传成实时视频生成。

**真流式（True Streaming）**：模型推理产生片段后立即发送，并具备取消、背压和恢复协议。  
_避免_：把完整 MP4 生成完之后再拆成两段发送称作真流式。
